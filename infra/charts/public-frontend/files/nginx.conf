# ─────────────────────────────────────────────────────────────────────────────
# public-frontend main nginx.conf — baked into the image at /etc/nginx/nginx.conf
#
# DPR-2: this replaces the upstream nginx default so we can:
#   • declare http{}-level zones (proxy_cache_path, limit_req_zone) at boot
#   • include /etc/nginx/conf.d/maps/*.conf for the bot-UA map (configmap)
#   • include /etc/nginx/conf.d/*.conf for the rendered server block + upstream
#
# All conf.d/* + conf.d/maps/* content is rendered by /entrypoint.sh from
# ConfigMap-supplied templates at container start.
# ─────────────────────────────────────────────────────────────────────────────

worker_processes auto;
pid /var/run/nginx/nginx.pid;

events {
    worker_connections 1024;
    multi_accept on;
}

http {
    # ── MIME / basic hardening ───────────────────────────────────────────────
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    server_tokens off;

    # ── Logging (stdout/stderr — see Dockerfile symlinks) ────────────────────
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" rt=$request_time uct="$upstream_connect_time" '
                      'uht="$upstream_header_time" urt="$upstream_response_time"';
    access_log  /var/log/nginx/access.log  main;
    error_log   /var/log/nginx/error.log   warn;

    # ── Performance ──────────────────────────────────────────────────────────
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;
    types_hash_max_size 2048;
    client_max_body_size 1m;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types
        application/javascript
        application/json
        application/xml
        application/rss+xml
        application/atom+xml
        image/svg+xml
        text/css
        text/plain
        text/xml;

    # ── http{}-level rate-limit zones ────────────────────────────────────────
    # FEAT-0005 (10 r/m per IP) applies to /api/public/v1/forms/* and other
    # forms endpoints. FEAT-0026 US-018 (2026-07-13) declares a tighter
    # zone that governs the CMS locations (/pages, /pages/{slug},
    # /redirects/{slug}, /pages/{slug}/og) at 120 r/m per IP with a
    # concurrent-connection cap so cache-bypass on the human path cannot
    # be weaponised for L7 abuse.
    limit_req_zone  $binary_remote_addr zone=public:10m          rate=10r/m;
    limit_req_zone  $binary_remote_addr zone=cms_public:10m      rate=120r/m;
    limit_conn_zone $binary_remote_addr zone=cms_conn:10m;

    # ── http{}-level proxy cache (sitemap 1h, OG 10m, list 5m via overrides) ─
    proxy_cache_path /var/cache/nginx/proxy
                     levels=1:2
                     keys_zone=public_cache:10m
                     max_size=200m
                     inactive=1h
                     use_temp_path=off;

    # ── Includes ─────────────────────────────────────────────────────────────
    # bot UA map MUST be included at http{} level, hence here (not conf.d).
    include /etc/nginx/conf.d/maps/*.conf;
    # rendered upstream + server blocks
    include /etc/nginx/conf.d/*.conf;
}
