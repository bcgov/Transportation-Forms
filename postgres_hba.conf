# PostgreSQL Client Authentication Configuration
# This file permits all connections without password for testing

# TYPE  DATABASE        USER            ADDRESS                 METHOD
# All connections use trust authentication (no password required)
local   all             all                                     trust
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                    trust
